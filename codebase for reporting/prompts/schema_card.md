# Schema Card — Oracle (owner: ATS)

Scope: ATS.SYMBOL_DAILY_INDICATORS, ATS.SYMBOL_INDICATORS, ATS.BEST_MKT, ATS.SYMBOLS, ATS.EXCHANGES, ATS.ORDERS, ATS.SYMBOL_MARKETS, ATS.FLAGS, ATS.ORDER_TYPES, ATS.ORDER_STATES, ATS.SUBSCRIBED_BROKERS, ATS.EDS_CLIENTS, ATS.USERS, ATS.TRADES, ATS.CANCELLED_TRADES, ATS.NEGOTIATED_TRADES, ATS.TRADE_STATES, ATS.SETTLEMENT_TYPES, ATS.SYMBOL_TYPES, ATS.REJECTED_ORDERS, ATS.REJECTIONS, ATS.ATS_LEVY_DATA_LOG, ATS.SETTLEMENT_LEVY_DATA, ATS.LEVIES, ATS.LEVY_TYPES, ATS.LEVY_RANGES, ATS.LEVY_CRITERIA, ATS.LEVY_INFO, ATS.LEVY_MEMBER_TYPES, ATS.LEVY_SECURITIES, ATS.REPO_CONTRACTS, ATS.REPO_CONTRACTS_CANCELLED, ATS.REPO_LOG


Oracle helpers:
  - Date buckets: TRUNC(<date_col>, 'DD'|'MM'|'YYYY')
  - Range filter: col >= DATE 'YYYY-MM-DD' AND col < DATE 'YYYY-MM-DD'
  - Row limit: FETCH FIRST <N> ROWS ONLY  (fallback: WHERE ROWNUM <= N)
  - Percentiles: PERCENTILE_CONT(p) WITHIN GROUP (ORDER BY expr)
  - Median: MEDIAN(expr)
  - Window: LAG/LEAD, AVG(...) OVER (PARTITION BY ... ORDER BY ... ROWS BETWEEN ...)
  - ANSI JOINs only; no legacy (+) joins.

Joins (preferred):

  - SDI ↔ SYMBOLS on (SYMBOL_DAILY_INDICATORS.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - SDI ↔ EXCHANGES on (SYMBOL_DAILY_INDICATORS.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - SI  ↔ SYMBOLS on (SYMBOL_INDICATORS.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - SI  ↔ EXCHANGES on (SYMBOL_INDICATORS.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - BM  ↔ SYMBOLS on (BEST_MKT.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - BM  ↔ EXCHANGES on (BEST_MKT.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - ORDERS ↔ SYMBOLS on (ORDERS.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - ORDERS ↔ EXCHANGES on (ORDERS.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - ORDERS ↔ SYMBOL_MARKETS on (ORDERS.SYMBOL_ID = SYMBOL_MARKETS.SYMBOL_ID AND ORDERS.MARKET_ID = SYMBOL_MARKETS.MARKET_ID AND ORDERS.EXCHANGE_ID = SYMBOL_MARKETS.EXCHANGE_ID)
  - ORDERS ↔ FLAGS on (ORDERS.FLAG_ID = FLAGS.FLAG_ID)
  - ORDERS ↔ ORDER_TYPES on (ORDERS.ORDER_TYPE = ORDER_TYPES.ID)
  - ORDERS ↔ ORDER_STATES on (ORDERS.STATE = ORDER_STATES.STATE_ID)
  - ORDERS ↔ SUBSCRIBED_BROKERS on (ORDERS.BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - ORDERS ↔ EDS_CLIENTS on (ORDERS.CLIENT_ID = EDS_CLIENTS.CLIENT_ID)
  - ORDERS ↔ USERS (as USER_ID) on (ORDERS.USER_ID = USERS.USER_ID)
  - ORDERS ↔ USERS (as TRADER_ID) on (ORDERS.TRADER_ID = USERS.USER_ID)
  - TRADES ↔ SYMBOLS on (TRADES.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - TRADES ↔ EXCHANGES on (TRADES.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - TRADES ↔ SYMBOL_MARKETS on (TRADES.SYMBOL_ID = SYMBOL_MARKETS.SYMBOL_ID AND TRADES.MARKET_ID = SYMBOL_MARKETS.MARKET_ID AND TRADES.EXCHANGE_ID = SYMBOL_MARKETS.EXCHANGE_ID)
  - TRADES ↔ SUBSCRIBED_BROKERS (buyer) on (TRADES.BUYER_BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - TRADES ↔ SUBSCRIBED_BROKERS (seller) on (TRADES.SELLER_BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - TRADES ↔ EDS_CLIENTS (buyer) on (TRADES.BUYER_CLIENT_ID = EDS_CLIENTS.CLIENT_ID)
  - TRADES ↔ EDS_CLIENTS (seller) on (TRADES.SELLER_CLIENT_ID = EDS_CLIENTS.CLIENT_ID)
  - TRADES ↔ USERS (buyer) on (TRADES.BUYER_USER_ID = USERS.USER_ID)
  - TRADES ↔ USERS (seller) on (TRADES.SELLER_USER_ID = USERS.USER_ID)
  - TRADES ↔ SETTLEMENT_TYPES on (TRADES.SETTLEMENT_TYPE_ID = SETTLEMENT_TYPES.SETTLEMENT_TYPE_ID)
  - TRADES ↔ TRADE_STATES on (TRADES.STATE_ID = TRADE_STATES.STATE_ID)
  - CANCELLED_TRADES ↔ EXCHANGES on (CANCELLED_TRADES.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - CANCELLED_TRADES ↔ SYMBOLS on (CANCELLED_TRADES.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - CANCELLED_TRADES ↔ SUBSCRIBED_BROKERS (buyer/seller) on (…BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - CANCELLED_TRADES ↔ EDS_CLIENTS (buyer/seller) on ((BUYER OR SELLER HERE BEFORE CLIENT ID)…CLIENT_ID = EDS_CLIENTS.CLIENT_ID) i.e for Buy side, BUYER_CLIENT_ID = EDS_CLIENTS.CLIENT_ID
  - [soft] CANCELLED_TRADES ↔ TRADES on ((BUYER_ORDER_NO, SELLER_ORDER_NO, EXCHANGE_ID) matching)
  - NEGOTIATED_TRADES ↔ SYMBOLS on (NEGOTIATED_TRADES.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - NEGOTIATED_TRADES ↔ EXCHANGES on (NEGOTIATED_TRADES.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - NEGOTIATED_TRADES ↔ SUBSCRIBED_BROKERS (sides) on (…BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - NEGOTIATED_TRADES ↔ EDS_CLIENTS (sides) on (…CLIENT_ID = EDS_CLIENTS.CLIENT_ID)
  - [soft] NEGOTIATED_TRADES ↔ TRADES on (ORDER_NO/COUNTER_ORDER_NO ↔ BUYER_ORDER_NO/SELLER_ORDER_NO, same EXCHANGE_ID)
  - REJECTED_ORDERS ↔ REJECTIONS on (REJECTED_ORDERS.REJECTION_ID = REJECTIONS.REJECTION_ID)
  - REJECTED_ORDERS ↔ SYMBOLS on (REJECTED_ORDERS.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - REJECTED_ORDERS ↔ EXCHANGES on (REJECTED_ORDERS.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - REJECTED_ORDERS ↔ SUBSCRIBED_BROKERS on (REJECTED_ORDERS.BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - REJECTED_ORDERS ↔ EDS_CLIENTS on (REJECTED_ORDERS.CLIENT_ID = EDS_CLIENTS.CLIENT_ID)
  - REJECTED_ORDERS ↔ USERS (as USER_ID) on (REJECTED_ORDERS.USER_ID = USERS.USER_ID)
  - REJECTED_ORDERS ↔ USERS (as TRADER_ID) on (REJECTED_ORDERS.TRADER_ID = USERS.USER_ID)
  - ATS_LEVY_DATA_LOG ↔ LEVIES on (ATS_LEVY_DATA_LOG.LEVY_ID = LEVIES.LEVY_ID)
  - ATS_LEVY_DATA_LOG ↔ LEVY_TYPES on (LEVIES.LEVY_TYPE_ID = LEVY_TYPES.ID)
  - ATS_LEVY_DATA_LOG ↔ SUBSCRIBED_BROKERS on (ATS_LEVY_DATA_LOG.BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - SETTLEMENT_LEVY_DATA ↔ LEVIES on (SETTLEMENT_LEVY_DATA.LEVY_ID = LEVIES.LEVY_ID)
  - SETTLEMENT_LEVY_DATA ↔ LEVY_TYPES on (LEVIES.LEVY_TYPE_ID = LEVY_TYPES.ID)
  - SETTLEMENT_LEVY_DATA ↔ SUBSCRIBED_BROKERS on (SETTLEMENT_LEVY_DATA.BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - SETTLEMENT_LEVY_DATA ↔ EDS_CLIENTS on (SETTLEMENT_LEVY_DATA.CLIENT_ID = EDS_CLIENTS.CLIENT_ID)
  - LEVY_RANGES ↔ LEVY_CRITERIA on (LEVY_RANGES.LEVY_CRITERIA = LEVY_CRITERIA.ID)
  - LEVY_INFO ↔ LEVIES on (LEVY_INFO.LEVY_ID = LEVIES.LEVY_ID)
  - REPO_CONTRACTS ↔ SYMBOLS on (REPO_CONTRACTS.SYMBOL_ID = SYMBOLS.SYMBOL_ID)
  - REPO_CONTRACTS ↔ EXCHANGES on (REPO_CONTRACTS.EXCHANGE_ID = EXCHANGES.EXCHANGE_ID)
  - REPO_CONTRACTS ↔ SUBSCRIBED_BROKERS (buyer/seller) on (…BROKER_ID = SUBSCRIBED_BROKERS.BROKER_ID)
  - REPO_CONTRACTS ↔ EDS_CLIENTS (buyer/seller) on (…CLIENT_ID = EDS_CLIENTS.CLIENT_ID)
  - REPO_CONTRACTS ↔ USERS (buyer/seller) on (…USER_ID = USERS.USER_ID)
  - REPO_CONTRACTS ↔ SETTLEMENT_TYPES on (REPO_CONTRACTS.SETTLEMENT_TYPE_ID = SETTLEMENT_TYPES.SETTLEMENT_TYPE_ID)
  - REPO_CONTRACTS_CANCELLED ↔ SYMBOLS/EXCHANGES (same as REPO_CONTRACTS)
  - REPO_LOG ↔ SYMBOLS / EXCHANGES / SUBSCRIBED_BROKERS / EDS_CLIENTS / USERS (by respective *_ID)
  - [soft] REPO_LOG ↔ REPO_CONTRACTS on (REPO_LOG.CONTRACT_ID = REPO_CONTRACTS.CONTRACT_NO)
  - [soft] REPO_CONTRACTS leg pairing on (same CONTRACT_NO; LEG = 1/2)

## ATS.SYMBOL_DAILY_INDICATORS  (grain: symbol_day)
  Columns:
    - STATS_DATE DATE  -- PK, NOT NULL
    - OPEN_PRICE NUMBER(14,4)  -- NOT NULL
    - CLOSE_PRICE NUMBER(14,4)  -- NOT NULL
    - LOW_PRICE NUMBER(14,4)  -- NOT NULL
    - HIGH_PRICE NUMBER(14,4)  -- NOT NULL
    - TURNOVER NUMBER(10,0)  -- NOT NULL
    - TURNOVER_VALUE NUMBER(20,4)
    - TRADES_COUNT NUMBER(10,0)  -- NOT NULL
    - AVERAGE_PRICE NUMBER(14,4)  -- NOT NULL
    - CHANGE_PRICE NUMBER(14,4)  -- NOT NULL
    - PERCENTAGE_CHANGE NUMBER(9,4)
    - MARKET_ID NUMBER(4,0)  -- PK, FK→ATS.MARKETS(MARKET_ID), NOT NULL
    - SYMBOL_ID NUMBER(15,0)  -- PK, FK→ATS.SYMBOLS(SYMBOL_ID), NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- PK, FK→ATS.EXCHANGES(EXCHANGE_ID), NOT NULL
    - OPEN_PRICE_YIELD NUMBER(15,4)
    - YIELD NUMBER(15,4)
  Date key: STATS_DATE
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  Measures:
  - turnover: SUM(TURNOVER_VALUE)
  - avg_price: AVG(CLOSE_PRICE)
  - open: OPEN_PRICE
  - high: HIGH_PRICE
  - low: LOW_PRICE
  - close: CLOSE_PRICE
  - trades_count: SUM(TRADES_COUNT)


## ATS.SYMBOL_INDICATORS  (grain: symbol_timestamp)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)  -- NOT NULL
    - OPEN NUMBER(14,4)  -- NOT NULL
    - HIGH NUMBER(14,4)  -- NOT NULL
    - LOW NUMBER(14,4)  -- NOT NULL
    - CLOSE NUMBER(14,4)  -- NOT NULL
    - AVG NUMBER(14,4)  -- NOT NULL
    - VOLUME NUMBER(10,0)  -- NOT NULL
    - VALUE NUMBER(15,4)
    - SYMBOL_ID NUMBER(15,0)  -- FK→ATS.SYMBOL_MARKETS(SYMBOL_ID), NOT NULL
    - MARKET_ID NUMBER(4,0)  -- FK→ATS.SYMBOL_MARKETS(MARKET_ID), NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- FK→ATS.SYMBOL_MARKETS(EXCHANGE_ID), NOT NULL
    - TRADES_COUNT NUMBER(10,0)
    - CHANGE_PRICE NUMBER(14,4)
    - OPEN_PRICE_YIELD NUMBER(15,4)
    - YIELD NUMBER(15,4)
  Date key: ENTRY_DATETIME
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  Measures:
  - volume: SUM(VOLUME)
  - turnover: SUM(VALUE)
  - avg_price: AVG(CLOSE)
  - open: OPEN
  - high: HIGH
  - low: LOW
  - close: CLOSE


## ATS.BEST_MKT  (grain: symbol_timestamp)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(9)  -- NOT NULL
    - BID_ORDER_COUNT NUMBER(8,0)
    - OFFER_ORDER_COUNT NUMBER(8,0)
    - BID_VOLUME NUMBER(10,0)
    - OFFER_VOLUME NUMBER(10,0)
    - BID_PRICE NUMBER(14,4)
    - OFFER_PRICE NUMBER(14,4)
    - SYMBOL_ID NUMBER(15,0)  -- NOT NULL
    - MARKET_ID NUMBER(4,0)  -- NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- NOT NULL
  Date key: ENTRY_DATETIME
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  Measures:
  - bid_price: AVG(BID_PRICE)
  - offer_price: AVG(OFFER_PRICE)
  - spread: AVG(OFFER_PRICE - BID_PRICE)
  - mid: AVG((BID_PRICE + OFFER_PRICE)/2)
  - bid_volume: SUM(BID_VOLUME)
  - offer_volume: SUM(OFFER_VOLUME)


## ATS.SYMBOLS
  Columns:
    - SYMBOL NVARCHAR2(64)  -- NOT NULL
    - SYMBOL_ID NUMBER(15,0)  -- PK, NOT NULL
    - COMPANY_ID NUMBER(15,0)  -- FK→ATS.COMPANIES(COMPANY_ID)
    - FACE_VALUE NUMBER(14,4)
    - OUTSTANDING_SHARES NUMBER(12,0)
    - SETTLEMENT_TYPE_ID NUMBER(4,0)  -- FK→ATS.SETTLEMENT_TYPES(SETTLEMENT_TYPE_ID), NOT NULL
    - SYMBOL_TYPE_ID NUMBER(2,0)  -- FK→ATS.SYMBOL_TYPES(SYMBOL_TYPE_ID), NOT NULL
    - SYMBOL_GROUP_ID NUMBER(4,0)  -- FK→ATS.SYMBOL_GROUPS(SYMBOL_GROUP_ID), NOT NULL
    - SYMBOL_CATEGORY_ID NUMBER(2,0)
    - ACTIVE NUMBER(1,0)  -- NOT NULL
    - CREATION_DATE DATE  -- NOT NULL
    - MODIFIED_BY VARCHAR2(32)  -- NOT NULL
    - MODIFIED_DATE DATE  -- NOT NULL
    - CURRENCY_ID NUMBER(4,0)  -- FK→ATS.CURRENCY(CURRENCY_ID), NOT NULL
    - FIRST_TRADING_DAY DATE  -- NOT NULL
    - ISSUE_PRICE NUMBER(14,4)  -- NOT NULL
    - ISIN NVARCHAR2(12)  -- NOT NULL
    - ISSUER_ID NUMBER(6,0)  -- FK→ATS.ISSUER(ISSUER_ID)
    - CFI NVARCHAR2(20)
    - FISN NVARCHAR2(35)
    - LEI NVARCHAR2(20)
    - HAIRCUT NUMBER(2,0)  -- NOT NULL
    - FREE_FLOAT_SHARES NUMBER(15,0)
    - AUTHORIZED_CAPITAL NUMBER(15,0)
    - ETF_TYPE_ID NUMBER(2,0)  -- FK→ATS.ETF_TYPE(ETF_TYPE_ID)


## ATS.EXCHANGES
  Columns:
    - EXCHANGE_CODE NVARCHAR2(8)  -- NOT NULL
    - EXCHANGE_NAME NVARCHAR2(128)  -- NOT NULL
    - CONTACT_ENTITY_ID NUMBER(10,0)  -- FK→ATS.CONTACT_ENTITIES(CONTACT_ENTITY_ID), NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- PK, NOT NULL
    - CONNECTIVITY NUMBER(1,0)  -- 0 = CAPAZAR , 1 = FIX 
    - CREATION_DATE DATE  -- NOT NULL
    - MODIFIED_BY VARCHAR2(32)  -- NOT NULL
    - MODIFIED_DATE DATE  -- NOT NULL
    - ACTIVE NUMBER(1,0)  -- NOT NULL
    - DESCRIPTION NVARCHAR2(500)
    - MBO_DEPTH NUMBER(4,0)  -- NOT NULL
    - MBP_DEPTH NUMBER(4,0)  -- NOT NULL
    - ALLOW_SHORT_SELL NUMBER(1,0)  -- NOT NULL
    - CURRENCY_ID NUMBER(4,0)  -- FK→ATS.CURRENCY(CURRENCY_ID)


## ATS.ORDERS  (grain: order_event)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)  -- PK, NOT NULL
    - ORDER_NO NUMBER(10,0)  -- PK, NOT NULL
    - VOLUME NUMBER(10,0)  -- NOT NULL
    - PRICE NUMBER(14,4)  -- NOT NULL
    - IS_SHORT NVARCHAR2(1)  -- NOT NULL
    - DISCLOSED_VOLUME NUMBER(10,0)  -- NOT NULL
    - SIDE NVARCHAR2(1)  -- NOT NULL
    - TIF DATE
    - REF_NO VARCHAR2(20)
    - TRIGGER_PRICE NUMBER(14,4)  -- NOT NULL
    - TRAILING_STOPLOSS_DIP NUMBER(14,4)  -- NOT NULL
    - CLIENT_ID NUMBER(20,0)  -- FK→ATS.EDS_CLIENTS(CLIENT_ID), NOT NULL
    - FLAG_ID NUMBER(2,0)  -- FK→ATS.FLAGS(FLAG_ID)
    - SYMBOL_ID NUMBER(15,0)  -- PK, FK→ATS.SYMBOL_MARKETS(SYMBOL_ID), NOT NULL
    - MARKET_ID NUMBER(4,0)  -- PK, FK→ATS.SYMBOL_MARKETS(MARKET_ID), NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- PK, FK→ATS.SYMBOL_MARKETS(EXCHANGE_ID), NOT NULL
    - BROKER_ID NUMBER(5,0)  -- FK→ATS.SUBSCRIBED_BROKERS(BROKER_ID), NOT NULL
    - USER_ID NUMBER(30,0)  -- FK→ATS.USERS(USER_ID), NOT NULL
    - STATE NUMBER(1,0)  -- NOT NULL
    - TRADER_ID NUMBER(30,0)  -- FK→ATS.USERS(USER_ID)
    - ORDER_TYPE NUMBER(1,0)  -- NOT NULL
    - PRICE_TYPE NUMBER(1,0)  -- NOT NULL
    - STATE_TIME TIMESTAMP(6)
    - QUOTE_ID NUMBER(10,0)
    - CUSTODIAN_ID NUMBER(20,0)
    - MF_VOLUME NUMBER(10,0)
    - QUEUING_TIME TIMESTAMP(6)
    - FIX_SESSION VARCHAR2(200)
    - ORIGINATOR NUMBER(1,0)
    - YIELD NUMBER(15,4)
    - ACCRUED_PROFIT NUMBER(24,6)
    - VALUE NUMBER(24,6)
    - PRICING_MECHANISM NUMBER(1,0)
    - SWIFT_REF_NO VARCHAR2(20)
    - INTERNAL_REF_NO VARCHAR2(25)
    - ACTUAL_VOLUME NUMBER(10,0)  -- NOT NULL
  Date key: ENTRY_DATETIME
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  - MARKET_ID
  - SIDE
  - FLAG_ID
  - ORDER_TYPE
  - STATE
  - CLIENT_ID
  - BROKER_ID
  - USER_ID
  - TRADER_ID
  Measures:
  - orders: COUNT(*)
  - volume: SUM(VOLUME)
  - notional_computed: SUM(VOLUME * PRICE)
  - value_stored: SUM(VALUE)
  - avg_price: AVG(PRICE)
  - avg_yield: AVG(YIELD)
  - accrued_profit: SUM(ACCRUED_PROFIT)
  Notes:
    • Use ENTRY_DATETIME for date filtering unless another timestamp is specified (STATE_TIME for state changes).
    • For generic notional, prefer SUM(VOLUME * PRICE). SUM(VALUE) (value_stored) may include instrument multipliers (esp. bonds).
    • SIDE is 'B'/'S'; IS_SHORT is a stringy boolean ('0'/'1').
--- 


## ATS.SYMBOL_MARKETS
  Columns:
    - TICK_SIZE NUMBER(8,4)  -- NOT NULL
    - BOARD_LOT NUMBER(6,0)  -- NOT NULL
    - UPPER_CIRCUIT_BREAKER_LIMIT NUMBER(14,4)  -- NOT NULL
    - LOWER_CIRCUIT_BREAKER_LIMIT NUMBER(14,4)  -- NOT NULL
    - UPPER_ORDER_VALUE_LIMIT NUMBER(15,4)  -- NOT NULL
    - LOWER_ORDER_VALUE_LIMIT NUMBER(15,4)  -- NOT NULL
    - UPPER_ORDER_VOLUME_LIMIT NUMBER(10,0)  -- NOT NULL
    - LOWER_ORDER_VOLUME_LIMIT NUMBER(10,0)  -- NOT NULL
    - MARKET_ID NUMBER(4,0)  -- PK, FK→ATS.MARKETS(MARKET_ID), NOT NULL
    - SYMBOL_ID NUMBER(15,0)  -- PK, FK→ATS.SYMBOLS(SYMBOL_ID), NOT NULL
    - FIFTY_TWO_WEEK_HIGH NUMBER(10,4)
    - FIFTY_TWO_WEEK_LOW NUMBER(10,4)
    - EXCHANGE_ID NUMBER(4,0)  -- PK, FK→ATS.EXCHANGES(EXCHANGE_ID), NOT NULL
    - CLOSE_PRICE NUMBER(16,4)
    - STATE NUMBER(1,0)  -- 0 = Open,1= Suspended, 2 = Deleted 
    - EXCHANGE_MARKET_ID NUMBER(10,0)  -- FK→ATS.EXCHANGE_MARKETS(EXCHANGE_MARKET_ID), NOT NULL
    - CREATION_DATE DATE  -- NOT NULL
    - MODIFIED_BY VARCHAR2(32)  -- NOT NULL
    - MODIFIED_DATE DATE  -- NOT NULL
    - MARKET_CONFIG_OVERRIDE NUMBER(1,0)  -- NOT NULL
    - UPPER_VOLUME_ALERT_LIMIT NUMBER(10,0)  -- NOT NULL
    - LOWER_VOLUME_ALERT_LIMIT NUMBER(10,0)  -- NOT NULL
    - UPPER_VALUE_ALERT_LIMIT NUMBER(15,4)  -- NOT NULL
    - LOWER_VALUE_ALERT_LIMIT NUMBER(15,4)  -- NOT NULL
    - MBO_DEPTH NUMBER(4,0)  -- NOT NULL
    - MBP_DEPTH NUMBER(4,0)  -- NOT NULL
    - ALLOW_SHORT_SELL NUMBER(1,0)  -- NOT NULL
    - UPPER_CIRCUIT_PERCENT NUMBER(3,0)  -- NOT NULL
    - LOWER_CIRCUIT_PERCENT NUMBER(3,0)  -- NOT NULL
    - QUOTE_SPREAD NUMBER(14,4)
    - REASON NVARCHAR2(160)
    - FIXED_TICK_SIZE NUMBER(1,0)
    - NEG_UPPER_CIRCUIT_BREAKER_LIMT NUMBER(14,4)
    - NEG_LOWER_CIRCUIT_BREAKER_LIMT NUMBER(14,4)
    - NEG_UPPER_CIRCUIT_PERCENT NUMBER(3,0)
    - NEG_LOWER_CIRCUIT_PERCENT NUMBER(3,0)
    - MIN_TRADE_VOLUME NUMBER(15,0)
    - NO_OF_DAYS NUMBER(10,0)
    - DYN_UPPER_CIRCUIT_PERCENT NUMBER(3,0)
    - DYN_LOWER_CIRCUIT_PERCENT NUMBER(3,0)
    - NEG_DYN_LOWER_CIRCUIT_PERCENT NUMBER(3,0)
    - NEG_DYN_UPPER_CIRCUIT_PERCENT NUMBER(3,0)


## ATS.FLAGS
  Columns:
    - FLAG_CODE NVARCHAR2(20)  -- NOT NULL
    - DESC_ NVARCHAR2(20)  -- NOT NULL
    - FLAG_ID NUMBER(2,0)  -- PK, NOT NULL


## ATS.ORDER_TYPES
  Columns:
    - ID NUMBER(4,0)  -- PK, NOT NULL
    - CODE VARCHAR2(20)  -- NOT NULL
    - DESCRIPTION VARCHAR2(100)


## ATS.ORDER_STATES
  Columns:
    - STATE_ID NUMBER(2,0)  -- PK, NOT NULL
    - STATE_CODE VARCHAR2(32)  -- NOT NULL


## ATS.SUBSCRIBED_BROKERS
  Columns:
    - BROKER_CODE NVARCHAR2(35)  -- NOT NULL
    - BROKER_ID NUMBER(5,0)  -- PK, NOT NULL
    - CASH NUMBER(15,0)
    - CREDIT_LINE_LIMIT NUMBER(20,4)
    - CM_ID NUMBER(5,0)
    - EXCHANGE_ID NUMBER(5,0)  -- FK→ATS.EXCHANGES(EXCHANGE_ID)
    - ACTIVE NUMBER(1,0)  -- NOT NULL
    - BYPASS_CASH_LIMIT NUMBER(1,0)
    - BYPASS_RISK NUMBER(1,0)
    - CREATION_DATE DATE  -- NOT NULL
    - MODIFIED_BY VARCHAR2(32)  -- NOT NULL
    - MODIFIED_DATE DATE  -- NOT NULL
    - MOBILE VARCHAR2(25)
    - EMAIL VARCHAR2(100)
    - BROKER_NAME NVARCHAR2(200)
    - BROKER_TYPE_ID NUMBER(3,0)  -- FK→ATS.BROKER_TYPES(BROKER_TYPE_ID), NOT NULL
    - BYPASS_CREDIT_LINE_LIMIT NUMBER(1,0)
    - BYPASS_SHORT_SELL NUMBER(1,0)
    - ADDRESS NVARCHAR2(264)
    - COUNTRY NVARCHAR2(64)
    - FAX NVARCHAR2(100)
    - TRADER_ID NUMBER(30,0)


## ATS.EDS_CLIENTS
  Columns:
    - CLIENT_CODE NVARCHAR2(30)  -- NOT NULL
    - AGENT_ID NUMBER(6,0)
    - CLIENT_ID NUMBER(20,0)  -- PK, NOT NULL
    - BRANCH_ID NUMBER(6,0)
    - BROKER_ID NUMBER(5,0)
    - CLIENT_GROUP_ID NUMBER(10,0)
    - DEFAULT_EXCHANGE_ID NUMBER(4,0)  -- FK→ATS.EXCHANGES(EXCHANGE_ID)
    - ACTIVE NUMBER(1,0)  -- 0 = Active, 1 = Suspended 
    - NAME NVARCHAR2(400)
    - MOBILE NVARCHAR2(30)
    - TELEPHONE NVARCHAR2(30)
    - NIC NVARCHAR2(30)
    - DEPO_REFERENCE NVARCHAR2(64)  -- NOT NULL
    - MODIFICATION_DATE DATE  -- NOT NULL
    - REGISTRATION_DATE DATE  -- NOT NULL
    - IS_FOREIGNER VARCHAR2(1)
    - ADDRESS NVARCHAR2(264)
    - EMAIL NVARCHAR2(200)
    - COUNTRY NVARCHAR2(64)
    - FAX NVARCHAR2(100)
    - IS_PROPRIETARY NUMBER(1,0)
    - IS_TAX_EXMP NUMBER(1,0)
    - IS_POLITICAL_PERSON CHAR(1)


## ATS.USERS
  Columns:
    - USER_ID NUMBER(30,0)  -- PK, NOT NULL
    - USER_NAME NVARCHAR2(32)  -- NOT NULL
    - PASSWORD NVARCHAR2(32)  -- NOT NULL
    - EMAIL NVARCHAR2(256)
    - USER_TYPE_ID NUMBER(3,0)  -- FK→ATS.USER_TYPES(TYPE_ID)
    - BROKER_ID NUMBER(5,0)  -- FK→ATS.SUBSCRIBED_BROKERS(BROKER_ID)
    - LAST_LOGIN_DATETIME DATE
    - LAST_LOGIN_IP NVARCHAR2(15)
    - SKIN NVARCHAR2(30)
    - STATUS NUMBER(2,0)  -- FK→ATS.USER_STATUS(ID) | 1 = Active, 0 = Suspended 
    - TWO_FACTOR_AUTH NUMBER(1,0)
    - PWD_OTP_FLAG NUMBER(1,0)
    - PWD_EXPIRY_DATE DATE  -- NOT NULL
    - CREATION_DATE DATE  -- NOT NULL
    - MODIFIED_BY VARCHAR2(32)  -- NOT NULL
    - MODIFIED_DATE DATE  -- NOT NULL
    - MOBILE VARCHAR2(15)
    - IP_ADDRESS VARCHAR2(20)
    - MAC_ADDRESS VARCHAR2(60)
    - COMPLETE_NAME VARCHAR2(64)  -- NOT NULL
    - SEND_SMS NVARCHAR2(1)  -- NOT NULL
    - LOGIN_SMS NVARCHAR2(1)  -- NOT NULL
    - SMS_AUTH_SUBSCRIPTION NVARCHAR2(1)  -- NOT NULL
    - USER_EXPIRY NVARCHAR2(1)


## ATS.TRADES  (grain: trade_event)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)  -- PK, NOT NULL
    - TICKET_NO NUMBER(10,0)  -- PK, NOT NULL
    - SELLER_ORDER_NO NUMBER(10,0)  -- NOT NULL
    - BUYER_ORDER_NO NUMBER(10,0)  -- NOT NULL
    - PRICE NUMBER(16,4)  -- NOT NULL
    - VOLUME NUMBER(10,0)  -- NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- PK, FK→ATS.SYMBOL_MARKETS(EXCHANGE_ID), NOT NULL
    - BUYER_CLIENT_ID NUMBER(20,0)  -- FK→ATS.EDS_CLIENTS(CLIENT_ID), NOT NULL
    - SELLER_CLIENT_ID NUMBER(20,0)  -- FK→ATS.EDS_CLIENTS(CLIENT_ID), NOT NULL
    - SYMBOL_ID NUMBER(15,0)  -- PK, FK→ATS.SYMBOL_MARKETS(SYMBOL_ID), NOT NULL
    - MARKET_ID NUMBER(4,0)  -- PK, FK→ATS.SYMBOL_MARKETS(MARKET_ID), NOT NULL
    - BUYER_BROKER_ID NUMBER(5,0)  -- FK→ATS.SUBSCRIBED_BROKERS(BROKER_ID), NOT NULL
    - SELLER_BROKER_ID NUMBER(5,0)  -- FK→ATS.SUBSCRIBED_BROKERS(BROKER_ID), NOT NULL
    - BUYER_USER_ID NUMBER(30,0)  -- FK→ATS.USERS(USER_ID), NOT NULL
    - SELLER_USER_ID NUMBER(30,0)  -- FK→ATS.USERS(USER_ID), NOT NULL
    - TRADE_TYPE NUMBER(1,0)  -- NOT NULL
    - IS_SHORT NVARCHAR2(1)  -- NOT NULL
    - STATE_ID NUMBER(2,0)  -- FK→ATS.TRADE_STATES(STATE_ID), NOT NULL
    - BUYER_REMAINING_VOLUME NUMBER(10,0)  -- NOT NULL
    - SELLER_REMAINING_VOLUME NUMBER(10,0)  -- NOT NULL
    - BUYER_CUSTODIAN_ID NUMBER(20,0)
    - SELLER_CUSTODIAN_ID NUMBER(20,0)
    - ACCRUED_PROFIT NUMBER(24,6)
    - YIELD NUMBER(15,4)
    - VALUE NUMBER(24,6)
    - PRICING_MECHANISM NUMBER(1,0)
    - PRICING_MODEL NUMBER(1,0)
    - SETTLEMENT_TYPE_ID NUMBER(4,0)
    - SETTLEMENT_AMOUNT NUMBER(24,6)
    - CONTRACT_ID VARCHAR2(25)
    - INITIAL_DATE DATE
    - TERMINATION_DATE DATE
    - LEG NUMBER(1,0)
    - REPURCHASE_PRICE NUMBER(16,4)
    - DIRTY_PRICE NUMBER(16,4)
    - BUYER_TRADER_ID NUMBER(30,0)
    - SELLER_TRADER_ID NUMBER(30,0)
  Date key: ENTRY_DATETIME
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  - MARKET_ID
  - BUYER_CLIENT_ID
  - SELLER_CLIENT_ID
  - BUYER_BROKER_ID
  - SELLER_BROKER_ID
  - BUYER_USER_ID
  - SELLER_USER_ID
  - BUYER_TRADER_ID
  - SELLER_TRADER_ID
  - STATE_ID
  - SETTLEMENT_TYPE_ID
  - TRADE_TYPE
  - IS_SHORT (It is either 1 or O)
  Measures:
  - trades: COUNT(*)
  - volume: SUM(VOLUME)
  - notional_computed: SUM(PRICE * VOLUME)
  - settlement_amount: SUM(SETTLEMENT_AMOUNT)
  - avg_price: AVG(PRICE)
  - avg_dirty_price: AVG(DIRTY_PRICE)
  - avg_yield: AVG(YIELD)
  - **accrued_profit: SUM(ACCRUED_PROFIT)**
  - avg_repurchase_price: AVG(REPURCHASE_PRICE)
  Notes:
    • TRADES has buyer_* and seller_* party columns; no explicit SIDE.
    • DIRTY_PRICE is clean+accrued (primarily bonds). SETTLEMENT_AMOUNT is the cash consideration.
    • Remaining volumes may be non-zero due to partial fills (do not assume zero).
    • Use (SYMBOL_ID, MARKET_ID, EXCHANGE_ID) to join venue info via SYMBOL_MARKETS when needed.
    - When asked about the Profit for the broker client or anything, Dont go towards summing the Value/Notional , Since its wrong. Always go towards the Sum of Accrued profit.


## ATS.CANCELLED_TRADES  (grain: cancel_event)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)  -- NOT NULL
    - TICKET_NO NUMBER(10,0)  -- NOT NULL
    - SELLER_ORDER_NO NUMBER(10,0)  -- NOT NULL
    - BUYER_ORDER_NO NUMBER(10,0)  -- NOT NULL
    - PRICE NUMBER(16,4)  -- NOT NULL
    - VOLUME NUMBER(10,0)  -- NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- NOT NULL
    - BUYER_CLIENT_ID NUMBER(20,0)  -- NOT NULL
    - SELLER_CLIENT_ID NUMBER(20,0)  -- NOT NULL
    - SYMBOL_ID NUMBER(15,0)  -- NOT NULL
    - MARKET_ID NUMBER(4,0)  -- NOT NULL
    - BUYER_BROKER_ID NUMBER(5,0)  -- NOT NULL
    - SELLER_BROKER_ID NUMBER(5,0)  -- NOT NULL
    - BUYER_USER_ID NUMBER(30,0)  -- NOT NULL
    - SELLER_USER_ID NUMBER(30,0)  -- NOT NULL
    - TRADE_TYPE NUMBER(1,0)  -- NOT NULL
    - IS_SHORT NVARCHAR2(1)  -- NOT NULL
    - STATE_ID NUMBER(2,0)  -- NOT NULL
    - BUYER_REMAINING_VOLUME NUMBER(10,0)  -- NOT NULL
    - SELLER_REMAINING_VOLUME NUMBER(10,0)  -- NOT NULL
    - BUYER_CUSTODIAN_ID NUMBER(20,0)
    - SELLER_CUSTODIAN_ID NUMBER(20,0)
    - ACCRUED_PROFIT NUMBER(20,4)
    - YIELD NUMBER(16,4)
    - VALUE NUMBER(20,4)
    - PRICING_MECHANISM NUMBER(1,0)
    - PRICING_MODEL NUMBER(1,0)
    - SETTLEMENT_TYPE_ID NUMBER(4,0)
    - SETTLEMENT_AMOUNT NUMBER(20,4)
    - CONTRACT_ID VARCHAR2(25)
    - INITIAL_DATE DATE
    - TERMINATION_DATE DATE
    - LEG NUMBER(1,0)
    - REPURCHASE_PRICE NUMBER(16,4)
    - DIRTY_PRICE NUMBER(16,4)
    - BUYER_TRADER_ID NUMBER(30,0)
    - SELLER_TRADER_ID NUMBER(30,0)
    - TRADE_DATETIME TIMESTAMP(6)
    - CANCELLATION_DATETIME TIMESTAMP(6)
  Date key: CANCELLATION_DATETIME
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  - MARKET_ID
  - BUYER_CLIENT_ID
  - SELLER_CLIENT_ID
  - BUYER_BROKER_ID
  - SELLER_BROKER_ID
  - BUYER_USER_ID
  - SELLER_USER_ID
  - STATE_ID
  - SETTLEMENT_TYPE_ID
  - TRADE_TYPE
  - IS_SHORT
  Measures:
  - cancellations: COUNT(*)
  - volume: SUM(VOLUME)
  Notes:
    • Treat as standalone cancellation events; do not assume 1:1 linkage to TRADES by TICKET_NO.
    • For exploratory linkage, try (BUYER_ORDER_NO, SELLER_ORDER_NO, EXCHANGE_ID) → TRADES.


## ATS.NEGOTIATED_TRADES  (grain: negotiation_event)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)  -- NOT NULL
    - ORDER_NO NUMBER(10,0)  -- NOT NULL
    - VOLUME NUMBER(10,0)  -- NOT NULL
    - PRICE NUMBER(14,4)  -- NOT NULL
    - IS_SHORT NVARCHAR2(1)  -- NOT NULL
    - DISCLOSED_VOLUME NUMBER(10,0)  -- NOT NULL
    - SIDE NVARCHAR2(1)  -- NOT NULL
    - TIF DATE
    - REF_NO VARCHAR2(20)
    - TRIGGER_PRICE NUMBER(14,4)  -- NOT NULL
    - TRAILING_STOPLOSS_DIP NUMBER(14,4)  -- NOT NULL
    - CLIENT_ID NUMBER(20,0)  -- NOT NULL
    - FLAG_ID NUMBER(2,0)
    - SYMBOL_ID NUMBER(15,0)  -- NOT NULL
    - MARKET_ID NUMBER(4,0)  -- NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- NOT NULL
    - BROKER_ID NUMBER(5,0)  -- NOT NULL
    - USER_ID NUMBER(30,0)  -- NOT NULL
    - STATE NUMBER(1,0)  -- NOT NULL
    - TRADER_ID NUMBER(30,0)
    - ORDER_TYPE NUMBER(1,0)  -- NOT NULL
    - COUNTER_ORDER_NO NUMBER(10,0)  -- NOT NULL
    - COUNTER_USER_ID NUMBER(30,0)  -- NOT NULL
    - COUNTER_CLIENT_ID NUMBER(20,0)
    - COUNTER_BROKER_ID NUMBER(5,0)  -- NOT NULL
    - NEGOTIATED_STATE NUMBER(1,0)  -- NOT NULL
    - PRICE_TYPE NUMBER(1,0)  -- NOT NULL
    - CUSTODIAN_ID NUMBER(20,0)  -- NOT NULL
    - COUNTER_CUSTODIAN_ID NUMBER(20,0)
    - YIELD NUMBER(15,4)
    - ACCRUED_PROFIT NUMBER(16,4)
    - VALUE NUMBER(20,4)
    - PRICING_MECHANISM NUMBER(1,0)
    - SETTLEMENT_TYPE_ID NUMBER(4,0)
    - SWIFT_REF_NO VARCHAR2(20)
    - NEGOTIATED_STATUS NUMBER(1,0)
    - INTERNAL_REF_NO VARCHAR2(25)
  Date key: ENTRY_DATETIME
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  - MARKET_ID
  - CLIENT_ID
  - COUNTER_CLIENT_ID
  - BROKER_ID
  - COUNTER_BROKER_ID
  - USER_ID
  - TRADER_ID
  - NEGOTIATED_STATE
  - NEGOTIATED_STATUS
  - ORDER_TYPE
  - PRICE_TYPE
  - IS_SHORT
  - SIDE
  Measures:
  - negotiations: COUNT(*)
  - volume: SUM(VOLUME)
  - notional_computed: SUM(PRICE * VOLUME)
  - avg_price: AVG(PRICE)
  - avg_yield: AVG(YIELD)
  - accrued_profit: SUM(ACCRUED_PROFIT)
  - value_stored: SUM(VALUE)
  Notes:
    • Empirically maps to executed TRADES via order numbers:
    •   (NEGOTIATED_TRADES.ORDER_NO or COUNTER_ORDER_NO) ↔ (TRADES.BUYER_ORDER_NO or SELLER_ORDER_NO) with same EXCHANGE_ID.
    • Do not assume linkage to ORDERS.ORDER_NO in this dataset.


## ATS.TRADE_STATES
  Columns:
    - STATE_ID NUMBER(2,0)  -- PK, NOT NULL
    - STATE_CODE VARCHAR2(32)  -- NOT NULL


## ATS.SETTLEMENT_TYPES
  Columns:
    - SETTLEMENT_TYPE NVARCHAR2(64)  -- NOT NULL
    - SETTLEMENT_DESC NVARCHAR2(256)
    - TRADE_DAYS NUMBER(3,0)  -- NOT NULL
    - SETTLEMENT_DAYS NUMBER(3,0)  -- NOT NULL
    - SETTLEMENT_TYPE_ID NUMBER(4,0)  -- PK, NOT NULL
    - ACTIVE NCHAR(1)  -- NOT NULL
    - CREATION_DATE DATE  -- NOT NULL
    - MODIFIED_BY VARCHAR2(32)  -- NOT NULL
    - MODIFIED_DATE DATE  -- NOT NULL


## ATS.SYMBOL_TYPES
  Columns:
    - SYMBOL_TYPE_ID NUMBER(2,0)  -- PK, NOT NULL
    - SYMBOL_TYPE NVARCHAR2(20)  -- NOT NULL


## ATS.REJECTED_ORDERS  (grain: rejection_event)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)  -- NOT NULL
    - ORDER_NO NUMBER(10,0)  -- NOT NULL
    - VOLUME NUMBER(10,0)  -- NOT NULL
    - PRICE NUMBER(14,4)  -- NOT NULL
    - IS_SHORT NVARCHAR2(1)  -- NOT NULL
    - DISCLOSED_VOLUME NUMBER(10,0)  -- NOT NULL
    - SIDE NVARCHAR2(1)  -- NOT NULL
    - TIF DATE
    - REF_NO VARCHAR2(20)
    - TRIGGER_PRICE NUMBER(14,4)  -- NOT NULL
    - TRAILING_STOPLOSS_DIP NUMBER(14,4)  -- NOT NULL
    - CLIENT_ID NUMBER(20,0)  -- NOT NULL
    - FLAG_ID NUMBER(2,0)
    - SYMBOL_ID NUMBER(15,0)  -- NOT NULL
    - MARKET_ID NUMBER(4,0)  -- NOT NULL
    - EXCHANGE_ID NUMBER(4,0)  -- NOT NULL
    - BROKER_ID NUMBER(5,0)  -- NOT NULL
    - USER_ID NUMBER(30,0)  -- NOT NULL
    - STATE NUMBER(1,0)  -- NOT NULL
    - TRADER_ID NUMBER(30,0)
    - ORDER_TYPE NUMBER(1,0)  -- NOT NULL
    - PRICE_TYPE NUMBER(1,0)  -- NOT NULL
    - REJECTION_ID NUMBER(2,0)
    - SOURCE_ID NUMBER(2,0)
    - STATE_TIME TIMESTAMP(6)
    - QUOTE_ID NUMBER(10,0)
    - CUSTODIAN_ID NUMBER(20,0)
    - YIELD NUMBER(15,4)
    - ACCRUED_PROFIT NUMBER(16,4)
    - VALUE NUMBER(20,4)
    - PRICING_MECHANISM NUMBER(1,0)
    - CLIENT_CODE NVARCHAR2(30)
    - INTERNAL_REF_NO VARCHAR2(25)
    - ACTUAL_VOLUME NUMBER(10,0)  -- NOT NULL
  Date key: ENTRY_DATETIME
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  - MARKET_ID
  - SIDE
  - FLAG_ID
  - ORDER_TYPE
  - CLIENT_ID
  - BROKER_ID
  - USER_ID
  - TRADER_ID
  - REJECTION_ID
  Measures:
  - rejected_orders: COUNT(*)
  - volume: SUM(VOLUME)
  - notional_computed: SUM(VOLUME * PRICE)
  - avg_price: AVG(PRICE)
  Notes:
    • Use ENTRY_DATETIME as the default date key for rejection analysis.
    • Join REJECTION_ID → REJECTIONS.DESCRIPTION for human-readable reasons.


## ATS.REJECTIONS
  Columns:
    - REJECTION_ID NUMBER(2,0)  -- PK, NOT NULL
    - DESCRIPTION VARCHAR2(250)  -- NOT NULL


## ATS.ATS_LEVY_DATA_LOG  (grain: levy_log_event)
  Columns:
    - TRANSACTION_ID NUMBER(14,0)
    - TRANSACTION_DATE DATE
    - TICKET_NO NUMBER(10,0)
    - BROKER_ID NUMBER(5,0)
    - ACCOUNT_ID NUMBER(20,0)
    - LEVY_ID NUMBER(8,0)
    - PRICE NUMBER(14,4)
    - VOLUME NUMBER(10,0)
    - AMOUNT NUMBER(15,4)
    - LEVY_AMOUNT NUMBER(14,4)
    - LEVY_RATE NUMBER(14,4)
    - BUY_SELL VARCHAR2(1)
    - MARKET VARCHAR2(14)
    - LEVY_ACCOUNT VARCHAR2(30)
    - FROM_LEVY_DATE DATE
    - TO_LEVY_DATE DATE
    - LOG_DESCRIPTION VARCHAR2(100)
    - LEVY_ENTRY_DATE TIMESTAMP(6)
    - ENTRY_USER VARCHAR2(30)
  Date key: TRANSACTION_DATE
  Dimensions:
  - LEVY_ID
  - BROKER_ID
  - ACCOUNT_ID
  - BUY_SELL
  - MARKET
  - LEVY_ACCOUNT
  Measures:
  - levy_amount: SUM(LEVY_AMOUNT)
  - rows: COUNT(*)
  - avg_levy_amount: AVG(LEVY_AMOUNT)
  Notes:
    • Operational levy log; may include entries without market/side (label as 'UNSPECIFIED' in outputs as needed).


## ATS.SETTLEMENT_LEVY_DATA  (grain: levy_settlement_event)
  Columns:
    - TRANSACTION_ID NUMBER(14,0)
    - TRANSACTION_DATE DATE
    - TICKET_NO NUMBER(10,0)
    - PRICE NUMBER(14,4)
    - VOLUME NUMBER(10,0)
    - AMOUNT NUMBER(15,4)
    - BROKER_ID NUMBER(5,0)
    - CLIENT_ID NUMBER(20,0)
    - LEVY_AMOUNT NUMBER(14,4)
    - LEVY_ID NUMBER(8,0)
    - BUY_SELL VARCHAR2(1)
    - MARKET VARCHAR2(14)
    - LEVY_ACCOUNT VARCHAR2(30)
    - LEVY_ENTRY_DATE TIMESTAMP(6)
    - ENTRY_USER VARCHAR2(30)
  Date key: TRANSACTION_DATE
  Dimensions:
  - LEVY_ID
  - BROKER_ID
  - CLIENT_ID
  - BUY_SELL
  - MARKET
  - LEVY_ACCOUNT
  Measures:
  - levy_amount: SUM(LEVY_AMOUNT)
  - rows: COUNT(*)
  Notes:
    • Settlement-stage levy facts; includes CLIENT_ID for client/broker pivots.
    - TRANSACTION_DATE, THIS is the date grain for this. Whenever asked about queries regarding the levi amounts of clients or similar queries, this table has this date grain which tells the date of that particular transaction


## ATS.LEVIES
  Columns:
    - LEVY_ID NUMBER(8,0)  -- PK, NOT NULL
    - LEVY_CODE VARCHAR2(32)  -- NOT NULL
    - LEVY_NAME VARCHAR2(128)  -- NOT NULL
    - EFFECTIVE_FROM DATE
    - APPLIES_TO VARCHAR2(4)
    - LEVY_TYPE_ID NUMBER(3,0)  -- FK→ATS.LEVY_TYPES(ID), NOT NULL
    - EFFECTIVE_TO DATE
    - RECURRENCE_CODE VARCHAR2(25)  -- FK→ATS.LEVY_RECURRENCE(RECURRENCE_CODE), NOT NULL
    - WITH_AGREEMENT VARCHAR2(1)
    - LEVY_CATEGORY VARCHAR2(12)  -- NOT NULL
    - OWN_PORTFOLIO VARCHAR2(1)
    - MAX_VALUE NUMBER(15,4)
    - MIN_VALUE NUMBER(15,4)
    - MARKET_ID NUMBER(10,0)  -- FK→ATS.MARKETS(MARKET_ID)
    - FOREIGN_CURRENCY_BOND NUMBER(1,0)


## ATS.LEVY_TYPES
  Columns:
    - ID NUMBER(3,0)  -- PK, NOT NULL
    - CODE VARCHAR2(20)
    - DESCRIPTION VARCHAR2(64)


## ATS.LEVY_RANGES
  Columns:
    - LEVY_RANGE_ID NUMBER(8,0)  -- PK, NOT NULL
    - LEVY_ID NUMBER(8,0)  -- FK→ATS.LEVIES(LEVY_ID), NOT NULL
    - RANGE_FROM NUMBER(15,4)  -- NOT NULL
    - RANGE_TO NUMBER(15,4)  -- NOT NULL
    - FIXED_RATE NUMBER(14,4)
    - LEVY_CRITERIA NUMBER  -- FK→ATS.LEVY_CRITERIA(ID), NOT NULL
    - VALUE_TYPE VARCHAR2(10)
    - MIN_VALUE NUMBER(14,4)
    - MAX_VALUE NUMBER(14,4)
    - PERCENT_OFF NUMBER(20,0)  -- FK→ATS.LEVY_PERCENT_OFF(ID)
    - PERCENT NUMBER(7,4)


## ATS.LEVY_CRITERIA
  Columns:
    - ID NUMBER  -- PK, NOT NULL
    - CRITERIA_CODE VARCHAR2(20)  -- NOT NULL
    - DESCREPTION VARCHAR2(25)


## ATS.LEVY_INFO
  Columns:
    - LEVY_ID NUMBER(8,0)  -- PK, FK→ATS.LEVIES(LEVY_ID), NOT NULL
    - VALUE_TYPE VARCHAR2(10)  -- NOT NULL
    - VALUE NUMBER(14,4)  -- NOT NULL
    - MIN_VALUE NUMBER(14,4)
    - MAX_VALUE NUMBER(14,4)
    - PERCENT_OFF NUMBER  -- FK→ATS.LEVY_PERCENT_OFF(ID)


## ATS.LEVY_MEMBER_TYPES
  Columns:
    - ID NUMBER(5,0)
    - MEMBER_TYPE_ID NUMBER(5,0)  -- FK→ATS.BROKER_TYPES(BROKER_TYPE_ID)
    - LEVY_ID NUMBER(5,0)  -- FK→ATS.LEVIES(LEVY_ID)
    - ACTIVE NUMBER(1,0)


## ATS.LEVY_SECURITIES
  Columns:
    - LEVY_ID NUMBER(10,0)  -- FK→ATS.LEVIES(LEVY_ID)
    - BOND_TYPE_ID NUMBER(10,0)
    - BOND_CATEGORY_ID NUMBER(10,0)  -- FK→ATS.BOND_CATEGORY(ID)
    - ID NUMBER(10,0)


## ATS.REPO_CONTRACTS  (grain: repo_leg)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)
    - CONTRACT_NO VARCHAR2(25)
    - SELLER_ORDER_NO NUMBER(10,0)
    - BUYER_ORDER_NO NUMBER(10,0)
    - PRICE NUMBER(16,4)
    - VOLUME NUMBER(10,0)
    - EXCHANGE_ID NUMBER(4,0)
    - BUYER_CLIENT_ID NUMBER(20,0)
    - SELLER_CLIENT_ID NUMBER(20,0)
    - SYMBOL_ID NUMBER(15,0)
    - MARKET_ID NUMBER(4,0)
    - BUYER_BROKER_ID NUMBER(5,0)
    - SELLER_BROKER_ID NUMBER(5,0)
    - BUYER_USER_ID NUMBER(30,0)
    - SELLER_USER_ID NUMBER(30,0)
    - TRADE_TYPE NUMBER(1,0)
    - STATE_ID NUMBER(2,0)
    - BUYER_REMAINING_VOLUME NUMBER(10,0)
    - SELLER_REMAINING_VOLUME NUMBER(10,0)
    - BUYER_CUSTODIAN_ID NUMBER(20,0)
    - SELLER_CUSTODIAN_ID NUMBER(20,0)
    - ACCRUED_PROFIT NUMBER(16,4)
    - YIELD NUMBER(15,4)
    - VALUE NUMBER(20,4)
    - PRICING_MECHANISM NUMBER(1,0)
    - PRICING_MODEL NUMBER(1,0)
    - SETTLEMENT_TYPE_ID NUMBER(4,0)
    - SETTLEMENT_AMOUNT NUMBER(20,4)
    - LEG NUMBER(1,0)
    - TERMINATION_DATE DATE
    - REPURCHASE_PRICE NUMBER(16,4)
    - TICKET_NO NUMBER(10,0)
    - INITIAL_DATE DATE
    - TRADE_STATE VARCHAR2(32)
    - IS_PROCESSED VARCHAR2(1)
    - DIRTY_PRICE NUMBER(16,4)
    - REPO_TYPE VARCHAR2(16)
    - REPURCHASE_ACCRUED_PROFIT NUMBER(16,4)
    - REPURCHASE_SETTLEMENT_AMOUNT NUMBER(20,4)
    - REPO_HAIRCUT NUMBER(8,4)
    - REPURCHASE_DIRTY_PRICE NUMBER(16,4)
    - REPO_RATE NUMBER(16,4)
    - PREVIOUS_CONTRACT_NO VARCHAR2(25)
    - NEW_CONTRACT_ID VARCHAR2(25)
    - CONTRACT_MODIFY_DATE DATE
    - PREVIOUS_REPURCHASE_AMOUNT NUMBER(20,4)
    - PAR_VALUE NUMBER(16,4)
    - MARKET_VALUE NUMBER(20,4)
    - ACCRUED_DAYS NUMBER
    - NEW_CONTARCT_ID VARCHAR2(25)

  Date semantics:
    1)  ENTRY_DATETIME — use for FLOW of NEW contracts (e.g. "repos created in the last N days").
    2) INITIAL_DATE / TERMINATION_DATE — use when you need contracts that are ACTIVE on a given analysis date D:
        INITIAL_DATE <= D AND TERMINATION_DATE > D
      (e.g. "repos active 500 days ago", "outstanding repos as of 2024-01-01").
    3) MATURITY_DATE — use for maturity buckets and outstanding-by-maturity analyses.

  Default date key (flows): ENTRY_DATETIME

  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  - MARKET_ID
  - BUYER_CLIENT_ID
  - SELLER_CLIENT_ID
  - BUYER_BROKER_ID
  - SELLER_BROKER_ID
  - BUYER_USER_ID
  - SELLER_USER_ID
  - LEG
  - REPO_TYPE
  - SETTLEMENT_TYPE_ID
  - STATE_ID
  Measures:
  - contract_legs: COUNT(*)
  - cash_flow: SUM(SETTLEMENT_AMOUNT)
  - notional_computed: SUM(PRICE * VOLUME)
  - avg_repo_rate: AVG(REPO_RATE)
  - avg_haircut: AVG(REPO_HAIRCUT)
  - avg_yield: AVG(YIELD)
  - accrued_profit: SUM(ACCRUED_PROFIT)
  Notes:
    • LEG pairs mirrored rows (1=initial, 2=repurchase).
    • Use INITIAL_DATE and TERMINATION_DATE for maturity buckets.
    • Prefer SETTLEMENT_AMOUNT for cash-flow; PRICE*VOLUME for generic notional.


## ATS.REPO_CONTRACTS_CANCELLED  (grain: repo_cancel_leg)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)
    - TRADE_DATETIME TIMESTAMP(6)
    - CONTRACT_NO VARCHAR2(25)
    - SELLER_ORDER_NO NUMBER(10,0)
    - BUYER_ORDER_NO NUMBER(10,0)
    - PRICE NUMBER(14,4)
    - VOLUME NUMBER(10,0)
    - EXCHANGE_ID NUMBER(4,0)
    - BUYER_CLIENT_ID NUMBER(20,0)
    - SELLER_CLIENT_ID NUMBER(20,0)
    - SYMBOL_ID NUMBER(15,0)
    - MARKET_ID NUMBER(4,0)
    - BUYER_BROKER_ID NUMBER(5,0)
    - SELLER_BROKER_ID NUMBER(5,0)
    - BUYER_USER_ID NUMBER(30,0)
    - SELLER_USER_ID NUMBER(30,0)
    - TRADE_TYPE NUMBER(1,0)
    - STATE_ID NUMBER(2,0)
    - BUYER_REMAINING_VOLUME NUMBER(10,0)
    - SELLER_REMAINING_VOLUME NUMBER(10,0)
    - BUYER_CUSTODIAN_ID NUMBER(20,0)
    - SELLER_CUSTODIAN_ID NUMBER(20,0)
    - ACCRUED_PROFIT NUMBER(16,4)
    - YIELD NUMBER(15,4)
    - VALUE NUMBER(15,4)
    - PRICING_MECHANISM NUMBER(1,0)
    - PRICING_MODEL NUMBER(1,0)
    - SETTLEMENT_TYPE_ID NUMBER(4,0)
    - SETTLEMENT_AMOUNT NUMBER(20,4)
    - LEG NUMBER(1,0)
    - TERMINATION_DATE DATE
    - REPURCHASE_PRICE NUMBER(14,4)
    - TICKET_NO NUMBER(10,0)
    - INITIAL_DATE DATE
    - TRADE_STATE VARCHAR2(32)
    - IS_PROCESSED VARCHAR2(1)
    - REPO_RATE NUMBER(8,4)
    - DIRTY_PRICE NUMBER(14,4)
    - REPO_TYPE VARCHAR2(16)
    - REPURCHASE_ACCRUED_PROFIT NUMBER(15,4)
    - REPURCHASE_SETTLEMENT_AMOUNT NUMBER(20,4)
    - REPO_HAIRCUT NUMBER(8,4)
    - REPURCHASE_DIRTY_PRICE NUMBER(14,4)
    - PREVIOUS_CONTRACT_NO VARCHAR2(25)
    - NEW_CONTRACT_ID VARCHAR2(25)
    - CONTRACT_MODIFY_DATE DATE
    - PREVIOUS_REPURCHASE_AMOUNT NUMBER(20,4)
    - PAR_VALUE NUMBER(16,4)
    - MARKET_VALUE NUMBER(20,4)
    - ACCRUED_DAYS NUMBER
  Date key: ENTRY_DATETIME, Semantics same as REPO CONTRACTS
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  - LEG
  - REPO_TYPE
  - STATE_ID
  Measures:
  - cancellations: COUNT(*)
  - volume: SUM(VOLUME)
  - cancelled_cash: SUM(SETTLEMENT_AMOUNT)
  Notes:
    • Structurally mirrors REPO_CONTRACTS; only voided legs.


## ATS.REPO_LOG  (grain: repo_log_event)
  Columns:
    - ENTRY_DATETIME TIMESTAMP(6)
    - ORDER_NO NUMBER(10,0)
    - VOLUME NUMBER(10,0)
    - PRICE NUMBER(16,4)
    - SIDE NVARCHAR2(1)
    - CONTRACT_ID VARCHAR2(20)
    - CLIENT_ID NUMBER(20,0)
    - SYMBOL_ID NUMBER(15,0)
    - MARKET_ID NUMBER(4,0)
    - EXCHANGE_ID NUMBER(4,0)
    - BROKER_ID NUMBER(5,0)
    - USER_ID NUMBER(30,0)
    - PRIVATE_ORDER_STATE NUMBER(2,0)
    - TRADER_ID NUMBER(30,0)
    - ORDER_TYPE NUMBER(1,0)
    - COUNTER_ORDER_NO NUMBER(10,0)
    - COUNTER_USER_ID NUMBER(30,0)
    - COUNTER_CLIENT_ID NUMBER(20,0)
    - COUNTER_BROKER_ID NUMBER(5,0)
    - NEGOTIATED_STATE NUMBER(1,0)
    - CUSTODIAN_ID NUMBER(20,0)
    - COUNTER_CUSTODIAN_ID NUMBER(20,0)
    - YIELD NUMBER(15,4)
    - ACCRUED_PROFIT NUMBER(16,4)
    - VALUE NUMBER(20,4)
    - PRICING_MECHANISM NUMBER(1,0)
    - SETTLEMENT_TYPE_ID NUMBER(4,0)
    - LEG NUMBER(1,0)
    - TERMINATION_DATE DATE
    - REPURCHASE_PRICE NUMBER(16,4)
    - PREVIOUS_REP_PRICE NUMBER(16,4)
    - REF_NO VARCHAR2(12)
    - INITIATOR NUMBER(1,0)
    - INITIAL_DATE DATE
    - PUBLIC_ORDER_STATE NUMBER(2,0)
    - NEGOTIATED_STATUS NUMBER(1,0)
    - REPO_RATE NUMBER(8,4)
    - SETTLEMENT_AMOUNT NUMBER(20,4)
    - DIRTY_PRICE NUMBER(16,4)
    - REPO_TYPE VARCHAR2(16)
    - REPURCHASE_ACCRUED_PROFIT NUMBER(16,4)
    - REPURCHASE_SETTLEMENT_AMOUNT NUMBER(20,4)
    - REPO_HAIRCUT NUMBER(8,4)
    - REPURCHASE_DIRTY_PRICE NUMBER(16,4)
    - NEW_CONTRACT_ID VARCHAR2(25)
    - CONTRACT_MODIFY_DATE DATE
    - PREVIOUS_REPURCHASE_AMOUNT NUMBER(20,4)
    - PAR_VALUE NUMBER(16,4)
    - MARKET_VALUE NUMBER(20,4)
    - ACCRUED_DAYS NUMBER
    - FIX_REF_NO VARCHAR2(20)
    - ORIGINATOR NUMBER(1,0)
    - FIX_SESSION VARCHAR2(200)
  Date key: ENTRY_DATETIME, Semantics Same as REPO CONTRACTS
  Dimensions:
  - SYMBOL_ID
  - EXCHANGE_ID
  - CLIENT_ID
  - COUNTER_CLIENT_ID
  - BROKER_ID
  - COUNTER_BROKER_ID
  - USER_ID
  - COUNTER_USER_ID
  - SIDE
  - LEG
  - NEGOTIATED_STATE
  - NEGOTIATED_STATUS
  Measures:
  - log_events: COUNT(*)
  - volume: SUM(VOLUME)
  - notional_computed: SUM(PRICE * VOLUME)
  - cash_flow: SUM(SETTLEMENT_AMOUNT)
  - avg_yield: AVG(YIELD)
  - avg_repo_rate: AVG(REPO_RATE)
  - avg_haircut: AVG(REPO_HAIRCUT)
  Notes:
    • Soft-join to REPO_CONTRACTS via CONTRACT_ID = CONTRACT_NO.
